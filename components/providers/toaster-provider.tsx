import { Toaster } from "react-hot-toast";

/**
 * ToastProvider component responsible for rendering toast notifications.
 * It simply renders the Toaster component from react-hot-toast library.
 */
export const ToastProvider = () => {
  return <Toaster />;
};
